---
import ToolLayout from '../layouts/ToolLayout.astro';
import TextExtract from '../components/tools/TextExtract.tsx';
---
<ToolLayout slug="text-extract">
  <TextExtract client:load />
  <div slot="seo-content">
    <h2>正则提取工具</h2>
    <p>
      这是 HyperGrad Text 提供的免费正则提取工具，从文本中批量提取邮箱、URL、手机号、IP、日期、身份证号等 12 种常见模式，
      支持自定义正则表达式、去重、计数。所有匹配在浏览器本地完成，不上传服务器，
      适合数据清洗、爬虫结果整理、文档信息抽取、表单数据校验场景。
    </p>
    <h3>12 种预设模式</h3>
    <ul>
      <li><strong>邮箱</strong>：RFC 5322 简化版，覆盖 99% 常见邮箱（gmail/outlook/qq/163/企业邮箱）。</li>
      <li><strong>URL</strong>：http/https 链接，含 query 与锚点。</li>
      <li><strong>手机号</strong>：中国大陆 <code set:html={'1[3-9]\\d{9}'}></code>，含 170/171/165 等虚拟运营商号段。</li>
      <li><strong>固定电话</strong>：区号-号码格式，如 010-12345678。</li>
      <li><strong>IPv4 / IPv6</strong>：标准 IP 地址格式。</li>
      <li><strong>日期 / 时间</strong>：YYYY-MM-DD、YYYY/MM/DD、YYYY年MM月DD日、HH:MM:SS 等常见格式。</li>
      <li><strong>身份证号</strong>：18 位含校验位（最后一个可能是 X）。</li>
      <li><strong>QQ 号 / 邮编 / MAC 地址</strong>：专用格式。</li>
    </ul>
    <h3>自定义正则</h3>
    <p>
      切到「自定义模式」选项卡，输入任意 JavaScript 正则表达式（带或不带 // 包裹），工具会执行 global 匹配并高亮所有命中位置。
      支持 flags（g/i/m/s/u/y）。语法错误时会给出错误提示。例如提取 4 位数字可输入 <code set:html={'\\d{4}'}></code>，提取 HTML 标签可输入 <code set:html={'<[^>]+>'}></code>。
    </p>
    <h3>为什么我的手机号没匹配上</h3>
    <p>
      预设手机号正则为 <code set:html={'1[3-9]\\d{9}'}></code>，要求前后是边界（空白或非数字）。如果手机号紧贴其他数字（如订单号 2021013812345678）会被吞掉一部分匹配。
      建议先用「文本清理」工具把数字字段用空格分隔，再提取。此外 170/171/165 等虚拟运营商号段已包含在 [3-9] 范围内。
    </p>
    <h3>去重与导出</h3>
    <p>
      开启「去重」后，重复匹配只保留一份。结果以列表形式展示，每行一个，可一键复制为纯文本列表，
      便于粘贴到 Excel、数据库导入或进一步处理。
    </p>
  </div>
</ToolLayout>
