---
import ToolLayout from '../layouts/ToolLayout.astro';
import WordCount from '../components/tools/WordCount.tsx';
---
<ToolLayout slug="word-count">
  <WordCount client:load />
  <div slot="seo-content">
    <h2>在线字数统计工具</h2>
    <p>
      这是 HyperGrad Text 提供的免费在线字数统计工具，专为中文写作、论文投稿、SEO 优化与自媒体排版场景设计。
      粘贴任意文本后，工具会实时计算「字符数（含空格）」「字符数（不含空格）」「中文字数」「英文单词数」「行数」「段落数」「UTF-8 字节数」「标点数」等多个指标，
      所有计算在浏览器本地完成，文本不上传服务器，适合统计合同、论文、商业文档等敏感内容。
    </p>
    <h3>中文字数到底怎么算</h3>
    <p>
      中文环境下「字数」有歧义。Word 的「字数」把每个汉字算 1 个字、每个英文单词算 1 个字；论文投稿通常看「字符数（含空格）」；自媒体平台（公众号、知乎）则按「纯汉字+标点」计算。
      本工具同时给出多种口径，你可以按场景选用：
    </p>
    <ul>
      <li><strong>含空格字符数</strong>：对应 Word「字符数(含空格)」，是大多数论文排版要求的指标。</li>
      <li><strong>中文字数</strong>：CJK 统一表意文字数量，每字 1，对应自媒体与中文论文字数。</li>
      <li><strong>英文单词数</strong>：连续字母算一个词，对应英文论文字数与英文 SEO 文案。</li>
      <li><strong>字节数 (UTF-8)</strong>：中文每字 3 字节，英文每字 1 字节，用于估算存储与传输大小。</li>
    </ul>
    <h3>SEO 与 meta description 用途</h3>
    <p>
      Google 中文搜索结果摘要约展示 75-80 个汉字，百度略多。SEO 写作时建议 meta description 控制在 140-160 字符（约 70-80 汉字）。
      本工具提供精确字符计数，可直接用于控制标题、描述、关键词长度，避免被搜索引擎截断。
    </p>
    <h3>与其他字数统计工具的区别</h3>
    <p>
      多数在线字数统计工具只给一个数字，且需要等待页面刷新。本工具采用实时统计架构（输入即更新），可自定义显示哪些指标，
      并通过 localStorage 自动静默保存当前内容（刷新不丢失）。所有文本处理均使用浏览器原生 TextEncoder 与正则，性能足够处理十万字级文档。
    </p>
  </div>
</ToolLayout>
